# 🗂️ Python File Sorter

A simple Python script that automatically sorts files in a directory into folders based on file type.

## ✨ Features
- Sorts files by extension (e.g. `.jpg`, `.pdf`, `.txt`)
- Automatically creates folders if they don’t exist
- Easy to customise and extend

## 🛠️ How It Works
1. Scans a target folder
2. Groups files by extension
3. Moves each file into a matching folder

For more projects go to [text](https://github.com/Munta2008/Muntaseer-Hossain)